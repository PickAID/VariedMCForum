{
    "name": "nodebb-plugin-extended-markdown",
    "version": "1.0.0",
    "description": "Extended Markdown plugin for NodeBB",
    "main": "library.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "shiki": "^1.0.0"
    },
    "peerDependencies": {
        "nodebb": ">=1.0.0"
    },
    "nbbpm": {
        "compatibility": "^3.0.0"
    }
}
